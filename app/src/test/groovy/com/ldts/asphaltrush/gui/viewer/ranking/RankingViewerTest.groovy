package com.ldts.asphaltrush.gui.viewer.ranking

import spock.lang.Specification;

class RankingViewerTest extends Specification {
    def "RankingViewer Test 1"() {
        setup:
        def result;

        when:
        result = 0;

        then:
        result != 0;
    }
}
