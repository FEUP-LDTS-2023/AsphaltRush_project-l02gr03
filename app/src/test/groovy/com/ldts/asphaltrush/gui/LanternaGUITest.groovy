package com.ldts.asphaltrush.gui

import spock.lang.Specification;

class LanternaGUITest extends Specification {
    def "Lanterna GUI Test"() {
        setup:
        def result;

        when:
        result = 0;

        then:
        result != 0;
    }
}
