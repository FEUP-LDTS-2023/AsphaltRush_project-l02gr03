package com.ldts.asphaltrush.rules.controller.garage

import spock.lang.Specification;

class GarageControllerTest extends Specification {
    def "GarageController Test 1"() {
        setup:
        def result;

        when:
        result = 0;

        then:
        result != 0;
    }
}
