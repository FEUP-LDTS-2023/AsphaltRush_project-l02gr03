package com.ldts.asphaltrush.model

import spock.lang.Specification;

class ImageFactoryTest extends Specification {
    def "ImageFactory Test 1"() {
        setup:
        def result;

        when:
        result = 0;

        then:
        result != 0;
    }
}
