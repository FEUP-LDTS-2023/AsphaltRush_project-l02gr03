package com.ldts.asphaltrush.controller.gameOver

import spock.lang.Specification;

class GameOverControllerTest extends Specification {
    def "GameOverController Test 1"() {
        setup:
        def result;

        when:
        result = 0;

        then:
        result != 0;
    }
}
