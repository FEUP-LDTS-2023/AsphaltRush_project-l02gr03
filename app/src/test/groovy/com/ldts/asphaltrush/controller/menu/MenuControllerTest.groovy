package com.ldts.asphaltrush.controller.menu

import spock.lang.Specification;

class MenuControllerTest extends Specification {
    def "MenuController Test 1"() {
        setup:
        def result;

        when:
        result = 0;

        then:
        result != 0;
    }
}
