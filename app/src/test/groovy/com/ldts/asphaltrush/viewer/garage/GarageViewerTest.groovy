package com.ldts.asphaltrush.gui.viewer.garage

import spock.lang.Specification;

class GarageViewerTest extends Specification {
    def "GarageViewer Test 1"() {
        setup:
        def result;

        when:
        result = 0;

        then:
        result != 0;
    }
}
