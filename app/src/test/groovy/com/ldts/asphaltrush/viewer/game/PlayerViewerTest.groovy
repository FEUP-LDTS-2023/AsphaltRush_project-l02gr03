package com.ldts.asphaltrush.viewer.game

import spock.lang.Specification;

class PlayerViewerTest extends Specification {
    def "PlayerViewer Test 1"() {
        setup:
        def result;

        when:
        result = 0;

        then:
        result != 0;
    }
}
